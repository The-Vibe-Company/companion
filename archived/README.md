# Archived Code

This directory keeps inactive historical snapshots.

## `legacy-companion/`

The previous Companion web UI codebase from before the Hermes fleet control-plane rewrite.

It is intentionally not wired into the root CI, release workflow, or deployment path. Treat it as reference material for now.
